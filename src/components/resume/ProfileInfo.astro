---
interface Props {
  title: string;
  text: string;
}

const { title, text } = Astro.props;
---

<div class="resume-container profile-container">
  <h2 style="text-align: center;">{title}</h2>
  <p>{text}</p>
</div>
